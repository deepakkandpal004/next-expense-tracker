'use client';

import { LandingPageContent } from '@/components/patterns/public-pages';

/** Public landing content rendered by the verified signed-out root route. */
export default function Guest() {
  return <LandingPageContent />;
}
